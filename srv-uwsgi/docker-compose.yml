version: "3.5"
services:
        pyapp:
          container_name: "pyapp"
          image: "srv-uwsgi"
          network_mode: "bridge"
          volumes:
            - "$PWD/pyapp:/var/pyapp:ro"
          ports:
            - "8001:8001"
