volumes:
  grafana_data:

services:
  grafana:
    container_name: "grafana"
    image: "docker.io/grafana/grafana:8.4.1"
    network_mode: "host"
    volumes:
      - "grafana_data:/var/lib/grafana:U"
    ports:
      - "3000:3000/tcp"
