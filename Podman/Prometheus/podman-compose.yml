volumes:
  prom_data:
  conf_data:

services:
  prometheus:
    image: "docker.io/prom/prometheus:v2.35.0"
    container_name: "prometheus"
    network_mode: "bridge"
    volumes:
      - "prom_data:/prometheus:U"
      - "conf_data:/config:ro"
    ports:
      - "9090:9090/tcp"

  vector:
    image: "docker.io/timberio/vector:0.21.0-alpine"
    container_name: "vector"
    network_mode: "bridge"
    volumes:
      - "conf_data:/config:ro"
    ports:
      - "8383:8383/tcp"
