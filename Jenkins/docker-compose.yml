version: "3.5"
services:
  jenkins:
    container_name: "srv-cicd"
    image: "jenkins/jenkins:lts"
    volumes:
      - "$PWD/jenkins:/var/jenkins_home:rw"
    ports:
      - "8080:8080"
      - "50000:50000"
