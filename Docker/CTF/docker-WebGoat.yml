networks:
  network_ctf:

services:
  webgoat:
    image: "webgoat/goatandwolf:v8.2.2"
    container_name: "webgoat"
    environment:
      - "TZ=Europe/Kiev"
    networks:
      - "network_ctf"
    ports:
      - "8080:8080/tcp"
      - "9090:9090/tcp"
