networks:
  core_network:
    external: true

services:
  owasp-wrongsecrets:
    image: "jeroenwillemsen/wrongsecrets:1.5.3-no-vault"
    container_name: "wrongsecrets.docker.localhost"
    networks:
      - "core_network"
    ports:
      - "8080:8080/tcp"
