version: "3.8"
services:
  jenkins:
    container_name: "jenkins-lts"
    image: "jenkins/jenkins:lts-alpine"
    network_mode: "bridge"
    volumes:
      - "$PWD/jenkins:/var/jenkins_home:rw"
    ports:
      - "8080:8080/tcp"
      - "50000:50000/tcp"
