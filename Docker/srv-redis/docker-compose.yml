version: "3.5"
services:
        redis:
          container_name: "redis"
          image: "srv-redis"
          network_mode: "bridge"
          volumes:
            - "$PWD/redis:/var/lib/redis:rw"
          ports:
            - "6379:6379"
