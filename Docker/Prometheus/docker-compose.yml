version: "3.5"
services:
  prometheus:
    image: "prom/prometheus:v2.31.1"
    container_name: "prometheus"
    network_mode: "bridge"
    volumes:
      - "$PWD/config:/etc/prometheus:ro"
      - "$PWD/storage:/prometheus:rw"
    ports:
      - "9090:9090"
