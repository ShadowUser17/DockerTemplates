version: "3.5"
services:
        mysql:
          container_name: "mysql"
          image: "srv-mysql"
          network_mode: "bridge"
          volumes:
            - "$PWD/mysql:/var/lib/mysql:rw"
          ports:
            - "3306:3306"
