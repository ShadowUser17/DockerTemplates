networks:
  core_network:
    external: true

services:
  vector:
    image: "timberio/vector:0.23.3-alpine"
    container_name: "vector-exporter"
    external_links:
      - "prometheus"
    volumes:
      - "$PWD/vector:/etc/vector:ro"
    networks:
      - "core_network"
    ports:
      - "8383:8383/tcp"
      - "9500:9500/tcp"
