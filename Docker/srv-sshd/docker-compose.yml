version: "3.8"
services:
  openssh:
    container_name: "srv-openssh"
    image: "python:openssh-bullseye"
    network_mode: "bridge"
    build:
      context: "$PWD"
      dockerfile: "dockerfile-python"
    ports:
      - "22:22/tcp"
