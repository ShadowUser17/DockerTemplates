networks:
  network_dns:

services:
  coredns:
    image: "coredns/coredns:1.9.3"
    container_name: "coredns"
    command:
      - "-conf=/data/Corefile"
    volumes:
      - "$PWD/data:/data:ro"
    networks:
      - "network_dns"
    ports:
      - "5300:5300/tcp"
      - "5300:5300/udp"
