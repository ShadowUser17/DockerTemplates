apiVersion: "v1"
kind: "Service"
metadata:
  namespace: "testing"
  name: "grafana-testing"
spec:
  selector:
    app.kubernetes.io/name: "grafana-testing"
  ports:
    - name: "http"
      protocol: "TCP"
      port: 3000
      targetPort: 3000
