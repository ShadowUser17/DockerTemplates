resources:
  - "base/access.yml"
  - "base/volume.yml"
  - "base/service.yml"
  - "base/deploy.yml"

namespace: "testing"

configMapGenerator:
  - name: "grafana-testing-datasources"
    files: ["datasources/testing.yml"]

images:
  - name: "grafana/grafana"
    newTag: "9.4.12"

labels:
  - pairs:
      app.kubernetes.io/part-of: "cluster-monitoring"
      app.kubernetes.io/managed-by: "kustomize"
