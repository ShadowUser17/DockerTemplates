apiVersion: "v1"
kind: "Service"
metadata:
  namespace: "testing"
  name: "vmetrics"
spec:
  selector:
    app.kubernetes.io/name: "vmetrics"
  ports:
    - name: "http"
      port: 8428
      protocol: "TCP"
      targetPort: 8428
