apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  namespace: "testing"
  name: "vmetrics-data"
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: "local-path"
  resources:
    requests:
      storage: "6Gi"
