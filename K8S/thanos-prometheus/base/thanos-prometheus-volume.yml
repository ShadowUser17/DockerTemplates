apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: "thanos-prometheus-data"
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: "longhorn"
  resources:
    requests:
      storage: "4Gi"
