apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  namespace: "loki"
  name: "minio-data"
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: "local-path"
  resources:
    requests:
      storage: "8Gi"
