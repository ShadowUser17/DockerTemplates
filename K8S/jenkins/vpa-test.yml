apiVersion: "autoscaling.k8s.io/v1"
kind: "VerticalPodAutoscaler"
metadata:
  name: "jenkins"
  namespace: "jenkins"
spec:
  targetRef:
    apiVersion: "apps/v1"
    kind: "Deployment"
    name: "jenkins"
  updatePolicy:
    updateMode: "Off"
