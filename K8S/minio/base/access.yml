apiVersion: "networking.k8s.io/v1"
kind: "NetworkPolicy"
metadata:
  name: "allow-from-all"
  namespace: "minio"
spec:
  policyTypes: ["Ingress"]
  podSelector:
    matchLabels:
      app.kubernetes.io/name: "minio"
  ingress:
    - from:
        - namespaceSelector: {}
