apiVersion: "v1"
kind: "Service"
metadata:
  namespace: "prometheus-cluster"
  name: "cadvisor-exporter"
spec:
  selector:
    app.kubernetes.io/name: "cadvisor-exporter"
  ports:
    - name: "http"
      protocol: "TCP"
      port: 8080
      targetPort: 8080
