apiVersion: "v1"
kind: "Service"
metadata:
  namespace: "prometheus"
  name: "grafana"
spec:
  selector:
    app.kubernetes.io/name: "grafana"
  ports:
    - name: "http"
      protocol: "TCP"
      port: 3000
      targetPort: 3000
