apiVersion: "v1"
kind: "Service"
metadata:
  namespace: "prometheus"
  name: "alertmanager"
spec:
  selector:
    app.kubernetes.io/name: "alertmanager"
  ports:
    - name: "http"
      protocol: "TCP"
      port: 9093
      targetPort: 9093
