apiVersion: "kustomize.toolkit.fluxcd.io/v1beta2"
kind: "Kustomization"
metadata:
  namespace: "flux-system"
  name: "prometheus-cluster"
spec:
  path: "./K8S/prometheus-cluster"
  prune: true
  interval: "1m0s"
  sourceRef:
    kind: "GitRepository"
    name: "docker-templates"
