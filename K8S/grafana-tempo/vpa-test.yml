apiVersion: "autoscaling.k8s.io/v1"
kind: "VerticalPodAutoscaler"
metadata:
  name: "grafana-tempo"
  namespace: "testing"
spec:
  targetRef:
    apiVersion: "apps/v1"
    kind: "Deployment"
    name: "grafana-tempo"
  updatePolicy:
    updateMode: "Off"
