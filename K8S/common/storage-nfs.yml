apiVersion: "v1"
kind: "PersistentVolume"
metadata:
  name: "storage-nfs"
  labels:
    type: "remote"
spec:
  capacity:
    storage: "12Gi"
  accessModes:
    - "ReadWriteMany"
  nfs:
    server: "k3s-nfs"
    path: "/nfs"
  mountOptions:
    - "nfsvers=4.0"
