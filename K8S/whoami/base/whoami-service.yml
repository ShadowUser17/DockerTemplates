apiVersion: "v1"
kind: "Service"
metadata:
  namespace: "whoami"
  name: "whoami"
spec:
  selector:
    app.kubernetes.io/name: "whoami"
  ports:
    - name: "http"
      protocol: "TCP"
      port: 4000
      targetPort: 4000
