apiVersion: "v1"
kind: "Service"
metadata:
  name: "thanos-query"
spec:
  selector:
    app.kubernetes.io/name: "thanos-query"
  ports:
    - name: "grpc"
      port: 10901
      targetPort: 10901
    - name: "http"
      port: 9090
      targetPort: 9090
