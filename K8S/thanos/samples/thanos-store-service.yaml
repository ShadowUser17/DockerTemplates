apiVersion: "v1"
kind: "Service"
metadata:
  name: "thanos-store"
spec:
  selector:
    app.kubernetes.io/name: "thanos-store"
  ports:
    - name: "grpc"
      port: 10901
      targetPort: 10901
    - name: "http"
      port: 10902
      targetPort: 10902
