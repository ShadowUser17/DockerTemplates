apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  namespace: "prometheus"
  name: "loki-testing-data"
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: "local-path"
  resources:
    requests:
      storage: "8Gi"
