apiVersion: "v1"
kind: "Service"
metadata:
  namespace: "testing"
  name: "loki-testing"
spec:
  selector:
    app.kubernetes.io/name: "loki-testing"
  ports:
    - name: "http"
      protocol: "TCP"
      port: 3100
      targetPort: 3100
