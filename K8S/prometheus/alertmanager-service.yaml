apiVersion: "v1"
kind: "Service"
metadata:
  namespace: "monitoring"
  name: "alertmanager-service"

spec:
  selector:
    app: "alertmanager"
  type: "LoadBalancer"
  ports:
    - name: "http"
      protocol: "TCP"
      port: 9093
      targetPort: 9093
