resources:
  - "base/service.yml"
  - "base/access.yml"
  - "base/deploy.yml"

configMapGenerator:
  - name: "prometheus-config"
    files:
      - "config/prometheus.yml"
      - "config/testing-rules.yml"

namespace: "testing"

images:
  - name: "prom/prometheus"
    newTag: "v2.47.0"

labels:
  - pairs:
      app.kubernetes.io/part-of: "cluster-monitoring"
      app.kubernetes.io/managed-by: "kustomize"
