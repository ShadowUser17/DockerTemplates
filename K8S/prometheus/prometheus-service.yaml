apiVersion: "v1"
kind: "Service"
metadata:
  namespace: "monitoring"
  name: "prometheus-service"

spec:
  selector:
    app: "prometheus"
  type: "LoadBalancer"
  ports:
    - name: "http"
      protocol: "TCP"
      port: 9090
      targetPort: 9090
