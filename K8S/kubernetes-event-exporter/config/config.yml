logLevel: "error"
logFormat: "json"
clusterName: "k3s"
route:
  routes:
    - match:
        - receiver: "loki"

receivers:
  - name: "loki"
    loki:
      url: "http://grafana-loki.testing.svc:3100/loki/api/v1/push"
      streamLabels: {}
