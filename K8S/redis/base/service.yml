apiVersion: "v1"
kind: "Service"
metadata:
  namespace: "testing"
  name: "redis"
spec:
  # type: "LoadBalancer"
  selector:
    app.kubernetes.io/name: "redis"
  ports:
    - name: "client"
      port: 6379
      protocol: "TCP"
      targetPort: 6379
