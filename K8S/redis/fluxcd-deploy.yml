apiVersion: "kustomize.toolkit.fluxcd.io/v1beta2"
kind: "Kustomization"
metadata:
  namespace: "flux-system"
  name: "redis"
spec:
  path: "./K8S/redis"
  prune: true
  interval: "1m0s"
  sourceRef:
    kind: "GitRepository"
    name: "docker-templates"
