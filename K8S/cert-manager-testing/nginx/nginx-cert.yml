apiVersion: "cert-manager.io/v1"
kind: "Certificate"
metadata:
  name: "nginx-site-cert"
  namespace: "cert-manager-testing"
spec:
  dnsNames: ["testing.k3s"]
  secretName: "nginx-cert"
  issuerRef:
    name: "test-ss-issuer"
    kind: "Issuer"
