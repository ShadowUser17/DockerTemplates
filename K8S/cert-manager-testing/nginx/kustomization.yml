resources:
  - "nginx-service.yml"
  - "nginx-deploy.yml"
  - "nginx-ingress.yml"

configMapGenerator:
  - name: "nginx-config"
    namespace: "cert-manager-testing"
    files:
      - "config/nginx.conf"

  - name: "nginx-site-config"
    namespace: "cert-manager-testing"
    files:
      - "config/root.conf"

  - name: "nginx-site-html"
    namespace: "cert-manager-testing"
    files:
      - "config/index.html"
