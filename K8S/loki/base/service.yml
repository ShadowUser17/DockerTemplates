apiVersion: "v1"
kind: "Service"
metadata:
  namespace: "prometheus"
  name: "loki"
spec:
  selector:
    app.kubernetes.io/name: "loki"
  ports:
    - name: "http"
      protocol: "TCP"
      port: 3100
      targetPort: 3100
